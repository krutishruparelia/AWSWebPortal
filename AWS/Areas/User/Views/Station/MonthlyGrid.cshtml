@using System.Data
@using AWS.Models
@{
    DataTable dt = new DataTable();
    dt = ViewBag.ColumnNames;
    DataTable dt1 = new DataTable();
    dt1 = ViewBag.month;

    <div class="card">
        <div class="table-responsive">
            <table class="table table-bordered" id="default-datatable">

                <col>
                <colgroup span="2"></colgroup>
                <colgroup span="2"></colgroup>
                <tr>
                    <td rowspan="2"></td>
                    @for (int i = 0; i < dt.Rows.Count - 1; i++)
                    {
                        AWSDatabaseContext db = new AWSDatabaseContext();
                        var sensorName = dt.Rows[i][0].ToString();
                        var getSensorIDsql = db.tbl_SensorMaster.Where(x => x.Name == sensorName).FirstOrDefault();
                        int sensorID = getSensorIDsql.ID;
                        var getUnitsql = db.tbl_ParameterMaster.Where(j => j.SensorID == sensorID).FirstOrDefault();
                        if (getUnitsql != null)
                        {
                            <th colspan="3" class="headcol" scope="colgroup" style="text-align:center;">@dt.Rows[i][0].ToString() (@getUnitsql.Unit)</th>
                        }
                        else
                        {
                            <th colspan="3" class="headcol" scope="colgroup" style="text-align:center;">@dt.Rows[i][0].ToString()</th>

                        }
                    }
                </tr>
                <tr>
                    @for (int i = 0; i < dt.Rows.Count - 1; i++)
                    {
                        <th scope="col">Min</th>
                        <th scope="col">Max</th>
                        <th scope="col">Avg.</th>
                    }
                </tr>
                @for (int i = 0; i < dt1.Rows.Count; i++)
                {
                    <tr>
                        <th scope="row">@dt1.Rows[i][0].ToString()</th>

                        @for (int j = 1; j < dt1.Columns.Count; j++)
                        {
                            <td>@dt1.Rows[i][j].ToString()</td>

                        }




                    </tr>
                }
            </table>
        </div>
    </div>

}

